DROP TABLE IF EXISTS mtcars;

CREATE TABLE mtcars(
  "makeModel" TEXT,
  "mpg" REAL,
  "cyl" INTEGER,
  "disp" REAL,
  "hp" INTEGER,
  "drat" REAL,
  "wt" REAL,
  "qsec" REAL,
  "vs" INTEGER,
  "am" INTEGER,
  "gear" INTEGER,
  "carb" INTEGER
);
